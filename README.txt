1) Make sure you have Java, Maven, Eclipse with TestNg plug in install
2) Please download  the latest selenium web driver and chromedirver (if you want to use chrome else we can use Firefox by default)
3) clone the repository and import as Maven project to IDE
5) mvn clean install
else 
6) Go to AEOTests.xml and run as TestNG Suite
7) Check for the results. 

